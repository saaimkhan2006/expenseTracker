# Point the frontend at the backend API.
# Local dev: leave commented out (Vite proxies same-origin /api → localhost:4000).
# Vercel + Render: set to your Render backend URL, must end in /api, no trailing slash.
# VITE_API_URL=https://smart-budget-api-xxxx.onrender.com/api
